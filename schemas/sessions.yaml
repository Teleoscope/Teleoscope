# Sessions
bsonType: object
title: session
required:
  - _id
  - user_id
  - expires_at
properties:
  _id:
    bsonType: string
    description: Lucia generated session id
  id:
    bsonType: string
    description: Lucia generated session id
  user_id:
    bsonType: string
    description: Lucia generated user id
  expires_at:
    bsonType: date
    description: The date when the session expires
  fresh:
    bsonType: bool
    description: Whether the session is fresh

