[pytest]
testpaths = tests
markers =
    integration: tests that need live services (MongoDB, etc.). Run with: pytest -m integration
    e2e: end-to-end tests (upload -> vectorize -> vector search). Require full stack: MongoDB, RabbitMQ, Milvus, dispatch/tasks/graph/vectorizer/uploader workers. Run with: pytest -m e2e
