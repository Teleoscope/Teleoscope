# .env — generated by Ansible for the GPU EC2 vectorizer stack.
# Re-generate: ansible-playbook -i vars/inventory.yaml ansible/deploy-vectorizer.yaml

# RabbitMQ on the main EC2 (private IP, same VPC).
RABBITMQ_HOST={{ ec2_main_private_ip }}
RABBITMQ_USERNAME={{ rabbitmq_dev_username }}
RABBITMQ_PASSWORD={{ rabbitmq_dev_password }}
RABBITMQ_PORT=5672
RABBITMQ_VHOST=/
RABBITMQ_VECTORIZE_QUEUE=teleoscope-vectorize
RABBITMQ_UPLOAD_VECTOR_QUEUE=teleoscope-upload-vector

# Vectorizer: always-on on a dedicated instance (monitor.py controls the EC2).
VECTORIZER_ALWAYS_ON=1
VECTORIZER_CONTROL_TOKEN={{ vectorizer_control_token | default('') }}
