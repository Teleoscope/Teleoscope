{
    "$jsonSchema": {
        "bsonType": "object",
        "title": "Search",
        "required": [
            "query"
        ],
        "properties": {
            "query": {
                "bsonType": "string",
                "description": "the string for the query"
            }
        }
    }
}