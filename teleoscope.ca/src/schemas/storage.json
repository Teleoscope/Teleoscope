{
    "$jsonSchema": {
        "bsonType": "object",
        "title": "storage",
        "required": [
            "label"
        ],
        "properties": {
            "label": {
                "bsonType": "string",
                "description": "the label of the data"
            }
        }
    }
}